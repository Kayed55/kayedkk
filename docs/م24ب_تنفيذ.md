# م24-ب — خطة التنفيذ خطوة بخطوة (مع نقاط توقف للمراجعة)

> **مبدأ:** كل شيء قابل للاسترجاع. لا `ALTER` قبل النسخ الاحتياطي. القارئات اليومية (create_evaluation…) **لا تُمَسّ** — الـtrigger يحافظ على التوافق.

---

## 🛑 نقطة توقف 0 — قبل البدء
- [ ] تأكيد **Supabase Auto Backup** مفعّل (نقطة استرجاع كاملة للقاعدة).
- [ ] مراجعة `security-migration/36_generalize_templates.sql` كاملاً (هذا الـPR).

## الخطوة 1 — النسخ الاحتياطي للجدول
```sql
-- backup_scripts/backup_templates_20260725.sql
CREATE TABLE IF NOT EXISTS public.evaluation_templates_backup_20260725 AS
  SELECT * FROM public.evaluation_templates;
```
**تحقّق:**
```sql
SELECT (SELECT count(*) FROM public.evaluation_templates) AS original,
       (SELECT count(*) FROM public.evaluation_templates_backup_20260725) AS backup;
```
### 🛑 نقطة توقف 1 — يجب `original = backup` قبل المتابعة.

## الخطوة 2 — تنفيذ ملف 36 (ذرّي: BEGIN…COMMIT واحد)
```
الصق security-migration/36_generalize_templates.sql كاملاً → Run
```
المتوقّع: `Success`. لو فشل أي أمر → **يتراجع كل شيء** (لا تغيير جزئي) → أرسل الخطأ.

**ماذا يفعل (بالترتيب داخل المعاملة):**
1. `ADD name, status` (nullable).
2. backfill: `name = coalesce(job_role,'الافتراضي')` · `status = (is_active?active:inactive)`.
3. قيود: `NOT NULL` + `CHECK(status)` + `UNIQUE(dept,name)`.
4. `trigger` مزامنة `is_active↔status`.
5. دوال: `list_templates` · `set_template_status` · `copy_template` · `delete_template`.

## الخطوة 3 — تحقّق ما بعد التنفيذ
```sql
-- (أ) الأعمدة + backfill سليم (لا NULL، أسماء فريدة داخل القسم)
SELECT department_id, name, status, is_active, job_role
FROM public.evaluation_templates ORDER BY department_id, name;

-- (ب) الدوال الأربع موجودة
SELECT proname FROM pg_proc WHERE pronamespace='public'::regnamespace
  AND proname IN ('list_templates','set_template_status','copy_template','delete_template');

-- (ج) اختبار المزامنة ثنائية الاتجاه (على نموذج غير حرج، ثم أعِده):
--   1) عبر status:
UPDATE public.evaluation_templates SET status='inactive' WHERE id=35;
SELECT id,status,is_active FROM public.evaluation_templates WHERE id=35;  -- متوقّع: inactive / false
--   2) عبر is_active (محاكاة RPC قديم):
UPDATE public.evaluation_templates SET is_active=true WHERE id=35;
SELECT id,status,is_active FROM public.evaluation_templates WHERE id=35;  -- متوقّع: active / true
--   (أعِد id=35 لحالته الأصلية active/true)
```
### 🛑 نقطة توقف 2 — يجب أن يتطابق `status` و`is_active` في كل الحالات + backfill بلا NULL.

## الخطوة 4 — اختبار عدم الانحدار (القارئات اليومية)
```sql
-- إنشاء تقييم محزم/CG من الواجهة يجب أن يعمل كالمعتاد (القارئات تعتمد is_active،
-- والـtrigger يبقيه متوافقاً). تحقّق أن التقييم يُنشأ ويستخدم القالب الصحيح.
```
### 🛑 نقطة توقف 3 — إنشاء تقييم يعمل (لا انحدار) قبل بناء واجهة م24-ج.

## الخطوة 5 — دمج PR م24-ب (توثيق في المستودع)
بعد نجاح الخطوات 1–4 على القاعدة → موافقتك → دمج squash.

---

## ما بعد م24-ب (لاحقاً)
- **م24-ج:** واجهة البطاقات + `create_template`/`update_template` (تحقّق jsonb خاص بالنوع).
- **م24-د:** قائمة الموظف مفلترة بالقسم عبر `list_templates`.
- **م24-هـ:** اختبارات عدم انحدار شاملة.
- **م24-و (اختياري):** نقل القارئات من `is_active` إلى `status` ثم إزالة `is_active` والـtrigger.

## 🔄 الاسترجاع الطارئ (إن لزم)
- إزالة القيود/الأعمدة الجديدة + استعادة من `evaluation_templates_backup_20260725` (مفصّل في رأس سكربت النسخ الاحتياطي)، أو نقطة Supabase Auto Backup.

## ⚠️ ملاحظات تصميمية للمراجعة
- **لا DEFAULT على `status`** عمداً — الـtrigger هو مصدر التزامن (تفادي تعارض default مع is_active في الإدراج القديم).
- **`archived` = `is_active false`** للقارئات القديمة (لا تستخدمه للتقييم) — سلوك صحيح.
- **`list_templates` يستثني `archived`** (المؤرشفة لا تظهر في الإدارة الافتراضية؛ يمكن إضافة فلتر لاحقاً).
- **`create_template`/`update_template` مؤجّلتان** لـم24-ج (مقترنتان بنماذج الواجهة) — أبلغني لو تريدهما هنا.
